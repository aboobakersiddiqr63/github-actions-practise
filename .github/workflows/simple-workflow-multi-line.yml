name: Simple Workflow Multi-Line Shell Command
on: [workflow_dispatch]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "First line"
          echo "Second line"
      
